# Paper AI Agent

논문과 뉴스를 수집·정제하고, Transformer 및 Hybrid RAG 기반 에이전트로 검색과 답변 생성을 수행하기 위한 프로젝트 골격입니다.

## 시작하기

1. `.env`과 `database/.env.example`을 복사해 로컬 `.env` 파일을 만듭니다.
2. 필요한 값을 입력합니다.
3. `docker compose up -d`로 서비스 구성을 시작합니다.

> 현재 저장소는 초기 디렉터리 구조와 자리표시자 설정만 포함합니다.
